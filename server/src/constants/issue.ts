export enum IssueType {
  release = 'release',
  story = 'story',
  bug = 'bug',
  task = 'task',
  test = 'test',
}

export enum IssueStatus {
  design = 'design',
  todo = 'todo',
  inProgress = 'inProgress',
  inReview = 'inReview',
  done = 'done',
  release = 'release',
  reOpen = 'reOpen',
}

export enum IssueComponents {
  Android = 'Android',
  IOS = 'IOS',
  'UX/UI' = 'UX/UI',
  QA = 'QA',
}

